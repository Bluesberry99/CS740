##this is lab1 for cs740
##Team members:
##1. Yijun Wang  (yjwang)
##2. Mengying Wang   (mengying)

##Assumptions:
##1. 1GB = 1000000000B

##Usage:
# Sever:  
# make & cd build 
# sudo ./lab1-server  

# Client: 
# make & cd build  
# sudo ./lab1-client <flow_num> <flow_size> > <output_file>

##meson version old
##so before step8 Build the DPDK
##need to pip install new meson first

# sudo apt-get install -y python3-pip ninja-build
# pip3 install --user meson==1.2.3
# echo 'export PATH=$HOME/.local/bin:$PATH' >> ~/.bashrc
# source ~/.bashrc
# meson --version
